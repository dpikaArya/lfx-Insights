"""Honest scoring layer.

Every score is a :class:`consilium.models.Score` that exposes its components,
weights, combination method, interpretation band, and uncertainty — never a bare
magic number. Shared helpers live in :mod:`consilium.scoring.common`.
"""
